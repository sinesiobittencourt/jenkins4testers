pipeline {
    agent any
        stages {
            stage("Build"){
                steps {
                    sh 'bundle install'
                     }
            }
        }                     
}